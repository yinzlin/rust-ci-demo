[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["fs"] } # 使用0.5版本，与axum 0.8兼容

[lib]
name = "backend"
path = "src/lib.rs"

[[bin]]
name = "app"
path = "src/main.rs"